<%= form_with url: "/applications/#{@application.id}", method: method, local: true do |f| %>
  <%= f.label passed_param, label_text  %>
  <%= f.text_field passed_param, value %>
  
  <%= f.submit button_text %>
<% end %>
